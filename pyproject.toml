[project]
name = "iblog-tools"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["md_translator"]

[tool.uv.sources]
md_translator = { workspace = true }

[tool.uv.workspace]
members = ["tools/md_translator"]
